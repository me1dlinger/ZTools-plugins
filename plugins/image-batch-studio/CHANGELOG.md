# Changelog

## 0.1.2 - 2026-09-01

- 适配 ZTools 3.2：截图导入、处理结果外拖，以及 Sharp runtime 使用 `pluginData` 专属目录；外拖限制为最近生成的普通文件并单次授权。
- Sharp runtime 迁移在完整校验后删除旧 `userData` 副本，避免卸载并清理数据后残留插件文件。
- 新增 2.4.0 宿主版本门禁；低版本或真实宿主版本不可识别时在业务 effect 前显示升级提示，仅浏览器开发预览放行。

## 0.1.1 - 2026-07-29

- 新增 Windows x64 与 ARM64 支持，首次使用时按当前系统架构安装 Sharp 运行组件。
- 修复 EXIF 方向图片的尺寸计算、裁剪和缩放结果，避免多帧 GIF 在普通处理流程中静默丢帧。
- 修复图片水印缺少文件、超出画布、模块开关失效及命名模板二次替换问题。
- 升级 Sharp 与 Vite，使用固定 URL、SHA-512 和原子安装固化动态运行组件。
- 将插件压缩包缩减至约 8MB，并增加 15MB 体积门禁、动态运行时 smoke 和 Windows CI 验证。

### 根据提交记录补充

- 修复 Windows 构建验证
- 通过 shell 启动 Windows npm 子进程，严格传播 CI 命令失败，并重试清理短暂锁定的 smoke 文件。
- 支持动态安装图像运行组件

## 0.1.0 - 2026-06-02

- 初始发布图片批处理插件，当前支持 macOS，Windows 适配开发中。
- 支持 Apple Silicon M 系列（arm64）和 Intel（x64）两类 macOS 设备，打包产物内置双架构 Sharp 运行时。
- 支持 JPG、JPEG、PNG、WebP、AVIF、HEIF、HEIC、TIFF、GIF 和 PDF 文件入口。
- 支持图片压缩、添加水印、格式转换、调整尺寸、图片裁剪、手动裁剪、图片旋转、图片翻转、添加边框和设置圆角。
- 支持合并 PDF、合并图片和合成 GIF。
- 所有图片处理、PDF 合并和 GIF 合成均在本地离线完成。
- 增加 typecheck、单元测试、macOS 双架构运行时检测、本地安装 smoke 和插件打包验证。

### 根据提交记录补充

- 修复符号链接循环扫描、GIF 透明索引、EXIF 方向、裁剪边界及水印处理问题。
- 优化图片扫描、像素缓存和拼图内存使用，限制超大输入及扫描数量。
- 完善输出命名、非覆盖原子写入及持久化设置校验，避免压缩后的 PNG 比原文件更大。
- 兼容 ZTools 3.0.1 数据目录与 ASAR 安装，并收窄 preload 服务接口。
