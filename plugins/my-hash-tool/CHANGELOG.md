# Changelog

## 1.3.2 - 2026-05-22

- 界面全面重构：设置页改为右侧抽屉，支持 ESC 关闭，主页面内容保持可见。
- 文件模式输入区改为紧凑标签列表，释放更多空间给结果区域。
- 批量进度面板改为内联轻量进度条，取消按钮移至进度条右侧。
- 全部小写/大写操作移至各结果卡片内，按需显示。
- 顶部操作栏精简为算法标注、复制全部结果、设置三个固定元素。
- 清空按钮与重新选择并排，位置更直观。
- 空状态新增图标和操作引导文案。
- Hash 结果行列宽改为自适应，算法名不再截断。
- 配色方案调整为靛蓝系：暖白背景、深靛蓝 accent，暗黑模式同步更新。
- 所有按钮字号统一为 13px，与页面整体排版协调。
- 进度条完成时颜色过渡平滑（蓝→绿加 transition）。
- 弹窗标题去掉「大文件提醒」eyebrow 标签，直接显示标题。

### 根据提交记录补充

- 清理无用逻辑，完善大小写切换说明与设计文档。

## 1.3.1 - 2026-05-17

- 计算结果新增大小写切换：每个算法行右侧提供独立切换按钮，头部新增「全部小写」「全部大写」批量入口。
- 设置页新增「默认大小写」选项，决定后续新计算结果的初始显示，并持久化保存。
- 复制行为遵循「所见即所复制」：单值复制、卡片复制全部、复制全部结果均按当前显示大小写输出。
- 切换为纯渲染层操作，不触发任何重新计算，未完成任务上的切换按钮保留焦点可达性。

### 根据提交记录补充

- 完善运行健壮性、生命周期处理及交互。

## 1.2.1 - 2026-05-13

- 优化暗黑模式下输入框聚焦态、批量进度面板和统计标签的视觉一致性。

### 根据提交记录补充

- 修正批量处理进度计算。
- 更新插件图标。

## 1.2.0 - 2026-05-13

- 增加可选额外算法：CRC32、SHA384、SHA512。
- 设置页支持开关额外算法，默认仍只启用 MD5、SHA1、SHA256。
- 文件计算保持单次流式读取，同时计算所有已启用算法。
- 复制单项和复制全部结果会包含当前任务启用的算法。

## 1.1.0 - 2026-05-13

- 优化整体 UI 风格与紧凑布局。
- 增加批量任务完成后的页面内轻提示。
- 设置插件窗口高度，改善默认打开体验。
- 修复计算中再次从 ZTools 文件入口进入会覆盖当前任务的问题。
- 修复长路径显示方向异常的问题。
- 增加一键打包 zip 的 npm 脚本。

## 1.0.0 - 2026-05-12

第一次提交

### 根据提交记录补充

- 修复插件审核反馈的问题。
- 支持批量复制哈希值。
- 精简主界面与设置页面交互。
